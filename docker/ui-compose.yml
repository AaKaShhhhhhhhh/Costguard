version: "3.8"
services:
  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    image: costguard/ui:local
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://localhost:8000}
    depends_on: []
    restart: unless-stopped
