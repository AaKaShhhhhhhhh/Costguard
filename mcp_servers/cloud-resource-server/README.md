# Cloud Resource Server

Placeholders for cloud resource control used by the `executor` agent.

Implement provider-specific clients that perform scaling and rightsizing
operations using least-privilege service accounts.
